# selenium-pom-student-portal
A Selenium WebDriver test automation framework built in Python to validate core workflows of a student portal application — login, form submission, and dashboard navigation. The framework follows the Page Object Model (POM) to separate test logic from UI locators, making tests easier to maintain as the application evolves.  
